# FMCPT Mini App hosting

Empacotamento vinext usado apenas para publicar `apps/mini_app` em HTTPS.

O comando `npm run sync:mini` copia os arquivos estáticos para `public/mini`.
Os comandos `npm run dev`, `npm run build` e `npm test` executam essa
sincronização automaticamente.
